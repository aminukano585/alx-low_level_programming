#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @p: first parameter
 * @q: second parameter
 * Return: int result of adding p and q
 */
int add(int p, int q)
{
	int result;

	result = p + q;

	return (result);
}
